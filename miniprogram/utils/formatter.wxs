var datetimeFormat = function (mills) {
  var date = getDate(mills);
  return date.getFullYear() + '-' + (date.getMonth() + 1) + '-' + date.getDate();
};

module.exports = {
  dtFmt: datetimeFormat,
};